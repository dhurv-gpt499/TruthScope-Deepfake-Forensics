# /home/gaurav/Documents/TruthScope/pyproject.toml
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "truthscope"
version = "4.0.0"
description = "Deepfake Detection Pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["core*", "utils*"]